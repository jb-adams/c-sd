/*
 * Purpose: operators overview
 * Author: Jeremy Adams
 * Date: 2019
 */

#include <stdio.h>
#include <stdbool.h>

int main(int argc, char *argv[]) {

    /*
     * operators + - * /
     * operand, the arguments to the symbol
     * logical operator returns true or false based on result of two or more
     *  expressions
     * 
     * assignment operator: =
     * relational operator: < > !=
     * bitwise operator: << >> ~
     * 
     * expressions and statements:
     * expression consists of operators and operands
     * most statements are constructed from expressions, a basic program step
     */
    
    return 0;
}